import XCTest
@testable import FluentOracleDriver

final class FluentOracleDriverTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FluentOracleDriver().text, "Hello, World!")
    }
}
